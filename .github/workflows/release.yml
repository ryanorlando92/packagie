name: Release Builder

on:
  push:
    tags:
      - 'v*' # This workflow only runs when you push a tag like "v1.0.0"

jobs:
  build-and-release:
    permissions:
      contents: write

    strategy:
      fail-fast: false
      matrix:
        platform: [ubuntu-22.04, windows-latest] # Add macos-latest if you are supporting Mac

    runs-on: ${{ matrix.platform }}
    
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4

      - name: Install dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libxdo-dev

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20 # Changed to 20/22 for wider compatibility, but keep 24 if your project requires it

      - name: Setup Rust compiler
        uses: dtolnay/rust-toolchain@stable

      - name: Install frontend NPM packages
        run: npm install

      - name: Build Tauri App and Publish Release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Packagie ${{ github.ref_name }}'
          releaseBody: 'Automated build generated by GitHub Actions.'
          releaseDraft: false
          prerelease: false